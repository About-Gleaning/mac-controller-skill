interface:
  display_name: "macOS Controller"
  short_description: "控制本机 macOS 应用，包括提醒事项和音乐"
  default_prompt: "Use $macos-controller to control local macOS apps, including creating or updating reminders and controlling Music playback."

policy:
  allow_implicit_invocation: true
